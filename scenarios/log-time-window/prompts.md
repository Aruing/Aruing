# log-time-window — 固定提示词

> 用 `aruing chat` 验收（beta17 logs 时间游标 smoke）。按下列顺序发；如 agent 反问或求确认，按 `expect.md` 方向自然续聊。

1. 「用 kubectl logs 加 --timestamps 看 demo 命名空间 demo-api 的完整日志，然后用 evidence.read 只切出最近一个重启周期之后那段」
