# same-name-multi-ns — 固定提示词

> 用 `aruing chat` 验收。**故意不带 namespace** 制造歧义。按下列顺序发；agent 若挂起问 ns，按真实意图回复（例如「team-b 的那个」），续聊。

1. 「demo-api 起不来是怎么回事」（不带 ns）
2. （若 agent 挂起问「哪个 ns / 哪个 demo-api」）回复：例如「team-b 的那个」
