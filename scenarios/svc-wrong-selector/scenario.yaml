name: svc-wrong-selector
kind_cluster_name: aruing-sc-svc-wrong-selector
namespace: demo
notes: "Service selector 故意拼错；Endpoints 应为空，Pod 正常 Running"
# 场景真值四元组：判分器（aruing judge）机械判分的依据；资源名与 manifests 实名一致
ground_truth:
  resource_type: service
  resource_name: demo-web
  namespace: demo
  fault_type: selector-mismatch
  fault_signature: [demo-web-typo, selector, 选择器]
