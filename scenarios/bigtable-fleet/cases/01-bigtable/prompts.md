# bigtable-fleet — 固定提示词

> 用 `aruing chat` 验收（0.1.4 map-reduce 步骤 4，完成标志 ①）。场景 `chat-env` 已注入
> `ARUING_TOOLS_PROJECTION_METHOD=map-reduce`（经 `make lab-chat` / `make smoke-all` 自动生效）。

1. 「看一下 fleet 命名空间里 appfleet 实例的整体健康状况：有没有异常实例？有的话大概多少、分布在哪、可能是什么原因？」
